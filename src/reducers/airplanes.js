import {} from '../actions/airplanes';

const initialState = {};

export default function airplaneState (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
